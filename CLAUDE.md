# CLAUDE.md — 接手開發前先讀這份

給後續的 agent（與人）看的**開發規矩**。這個 repo 是 three.js 的 voxel 建造／破壞遊戲，
單人維護、沒有建置步驟、產品就是那幾支 `.js` 加 `index.html`。

## 三份文件的分工

| | 裝什麼 | 什麼時候要改它 |
|---|---|---|
| `README.md` | 產品說明：玩法、操作、檔案、怎麼跑 | 玩法／檔案／指令變了才改 |
| `開發筆記.md` | **每個決定的來龍去脈**，一萬多行 | **每次做完一件事都要補**（見〈寫文件〉） |
| `CLAUDE.md`（這份） | 開發規矩與流程 | 規矩變了才改 |

**動手前先 grep `開發筆記.md`。** 這支程式的每一個怪寫法幾乎都有理由，而且理由都寫下來了。
要改什麼就拿關鍵字去搜（道具名、函式名、`v1.176` 這種版號都可以），
搜到的段名就是程式註解裡 `見 開發筆記〈…〉` 指的那個。
**先讀了再改**，不然很容易把某一版辛苦修好的東西改回去。

## 一輪是怎麼跑的

使用者通常丟一句話（「飛龍也要像獅鷲能做出地面行走」、「動物好像只會在地標周圍移動?」）。
最近這幾版都是這個節奏：

1. **先量再答，不要臆測。** 「好像只會在地標周圍移動」這種要先寫探針把數字量出來
   （動物實際待的半徑 18.8~25.8、碎料場外緣 63.7），再回答是不是真的、為什麼。
   量到的數字就是之後 commit 訊息與 `開發筆記.md` 的內容。**沒有證據就說查不到，不要用推測補。**
2. **會影響做法的選擇一次問完**（幾隻？範圍到哪？速度怎麼給？），不要自己假設一個就做下去。
3. **造型／動作類先給看**：產一頁預覽（`tools/.e2e-out/*.html`）請使用者過目，他點頭再落地。
4. **精準打擊**：只改目標範圍，不要順手重構不相干的地方。
5. 實作 → `--tier commit` 跑**一輪** → 綠了就 commit（版本號要動）。
6. **收尾報告**：調查結論與實據、改法、踩到的回歸、驗收數字，
   還有**什麼沒做**（例如「完整輪沒跑，照規矩要你說了才跑」）。

## 語言

產出的文件、註解、commit 訊息一律**繁體中文**。程式識別字照現有風格（英文短名）。

唯一的例外是 **`.bat` 裡只能有 ASCII**（註解、`echo`、`set /p` 都算）：`cmd.exe` 會把
UTF-8 中文解析壞，雙擊之後先噴幾行 `is not recognized` 才開始跑
（見〈`.bat` 裡不能寫中文〉）。要給人看的中文一律讓 `.py` 印，別把 `.bat` 改回中文。

## 版本號：三個地方要一致

**每次 commit 都要動版本號**，一律三段 `x.y.z`：一般改動 `patch +1`，功能性改動 `minor +1`（patch 歸零）。

| 地方 | |
|---|---|
| `src/game.js` 的 `VERSION` | 本尊 |
| `README.md` 第一行 | e2e 有一條比對 |
| `藍圖預覽.html` 的 `VIEWER_VER` | 那一頁不載遊戲層，e2e 另一條比對 |

（`開發筆記.md` 的抬頭也順手跟著寫，但沒有測試守著。）

## commit

- **改好測好可以自己 commit**，不必每次問。
- **但造型／動作類的改動要先給使用者看過再落地**（他要先看到長什麼樣子）。
  做法是產一頁 `tools/.e2e-out/*.html` 預覽（那個資料夾已 gitignore）給他開。
- **不要 push。** `origin` 是 GitHub 上的 `a0983610/block-builders`，
  但本地 `master` 沒設 upstream，而且已經領先 `origin/master` 好幾個 commit——
  要不要推上去是使用者的決定，他沒說就不要推。
- **不要 `git stash`**（要拿舊版對照就用 `git worktree`）。
- 訊息格式照現有的：標題一句話講做了什麼＋`（vX.Y.Z）`，內文寫
  「使用者要求 → 量到什麼 → 怎麼改 → 驗收數字」，最後一行固定：

  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  ```

## 測試與驗收

```
node tools/e2e-3d.cjs --tier must      改一行先跑這個（完整輪的 1/4）
node tools/e2e-3d.cjs --tier commit    commit 前跑這個（約 4 分鐘）
node tools/e2e-3d.cjs                  完整輪（7~8 分鐘）
node tools/e2e-3d.cjs --until 段名     開發用：跑到那一段就收工
node tools/e2e-3d.cjs --list           印段落一覽（段名／等級／行數），段名直接餵給 --until
node tools/check-bp.cjs --all          48 座藍圖體檢（幾秒）
tools\run-tests.bat                    不想打指令：雙擊出選單（參數也吃，直接接過去）
```

**規矩（v1.183 使用者定案，原話在 `開發筆記.md`〈驗收規矩〉）：**

1. **自己 commit ＝ 跑 `--tier commit`**，必要時加 `check-bp --all`。
2. **完整輪是使用者說要跑才跑**，不是每次 commit 的預設動作。
3. **一輪綠就算完成。** 不要為了保險連跑好幾輪、換種子掃、或開 `git worktree`
   跟上一版做 A/B——那些很貴（一次 A/B 十五分鐘起跳），**要先問過使用者**。

### 紅了怎麼查

照它印出來的種子重跑那一段：`--seed N --until 段名`。

- **重現得出來** → 程式真的壞了，**修程式**。
- **重現不出來** → 那條測試在賭骰子，**修量測、不動門檻**。
  放寬門檻不算修——那只是把不懂的東西藏起來。正確做法是把量測做穩
  （拉長取樣、改量比例而不是絕對值、排除不相干的個體），做法見
  `開發筆記.md`〈九條「偶爾飄」的測試〉。
- 有三成條目**同種子重跑也會換一副骰子**（見〈`--seed` 釘不住的那三成〉），
  那些都在 `tools/e2e-varying.json` 裡；遇到就當「重現不出來」處理。

### 改測試時一定要記得的四件事

0. **新增一段要標等級**：`head('段名', 等級)` 的第二個參數，段落本體包在 `SEC: { … }` 裡
   （跳過就 `break` 出去，本體沿用原縮排、不重排才不會生出整檔 diff）。
   硬規則：**某一段宣告、別段才讀的變數，宣告那一段的等級不能比讀它的那一段高**，
   否則跳掉之後那個名字會變成 `undefined`（〈整體〉那一段在守）。
1. **條目改名 → `tools/e2e-varying.json` 的鍵要手動搬。**
   鍵是 `段名 │ 條目名`（全形豎線），改了段名或條目名而沒搬，那一條就從浮動清單掉出來，
   `--tier commit` 會開始偶爾擋你。對帳法：拿清單裡的名字去比對測試檔所有 `ok('…')`
   的字面名（正常會有幾條對不上，因為那幾個名字是字串組出來的）。
2. **故意改造型 → `node tools/e2e-3d.cjs --update-models --until 造型基準`**，
   然後**看 `git diff` 確認變的就是你要改的那幾塊**（`tools/model-baseline.json` 進 git）。
3. **不要寫死會隨改動變動的數字。** 期望值一律讀常數算出來
   （見〈不要寫死會隨改動變動的數字〉）；測試動過的全域狀態要還回去
   （見〈測試動過的全域狀態要還回去〉）。

## 程式擺哪裡

```
src/engine.js        只管怎麼畫：three 場景、光影、相機、InstancedMesh 積木池、BEASTS 造型表
src/game.js          ①核心：版本、常數、狀態、音效、空間雜湊、積木、藍圖與積木池、整地
src/game-workers.js  ②小人：施工、逃命、慶祝、閒晃、工程師、魔法師、閒聊、閒晃事件、村子、偷懶
src/game-save.js     ③紀錄、成就、存檔（換場門檻 WRECK_AT 也在這）
src/game-tools.js    ④破壞道具與特效、天災與吉祥物、閒逛動物
src/game-ui.js       ⑤樹、主迴圈、輸入、HUD、面板、匯入建築、啟動（boot）
src/blueprints.js    48 座 voxel 產生器（資料，不依賴 three）
```

五支遊戲層是 **classic script 共用同一份全域 scope**，不是 ES module——
`file://` 載 ES module 會被 CORS 擋，而且測試大量直接改頂層 `let`。
所以拆檔跟寫在同一支檔裡完全等價，**不要改成 module**，
每一支都要自己寫 `'use strict';`（有一條測試在掃）。

## 常見任務的流程

**加一把破壞道具**：在 `game-tools.js` 的 `TOOLS` **接在最後面**（解鎖門檻是等差階梯
自動算的，不必挑數字），寫 `tip`（那就是給玩家看的說明）→ 實作 `useTool` 那條分支 →
e2e 加一段 → `README.md` 的道具名單加一個字 → `開發筆記.md`〈破壞道具〉補一節。

**加一款動物**：`engine.js` 的 `BEASTS` 加一組造型（一支 `xxxParts(o)` 函式開一款，
`bmir()` 鏡射右→左並反相 `sw`）→ 註冊進 `MODELS` → `game-tools.js` 的 `HERD_KIND`
與 `HERD_WALK` 各加一行（腿擺倍率是算出來的，只要給速度）→ `--update-models` 重產基準
→ **先做一頁預覽給使用者看造型**，他點頭再往下走。

**加一座內建藍圖**：`src/blueprints.js` 加一個吃 `s` 的產生器；積木數靠 `fitScale()` 掃，
不要自己套公式。**自訂藍圖**則是 `blueprints/` 加一支 `.js` ＋ `list.js` 加一行。

**改〈藍圖製作說明.md〉**：改完要跑 `node tools/build-bpdoc.cjs` 重新包成 `src/bpdoc.js`
（那份說明就是給 AI 的 prompt 本體，兩邊不同步的話預覽頁給出去的 prompt 是舊的）。

**效能相關**：規矩是「**沒東西在場就不吃 draw call**」（`visible = false`，不是 count 0），
有測試守著。量的是 CPU 端的 `step`／`draw`，headless 的 fps 沒有參考價值。

**要出一包給別人玩**：`python tools/export-game.py`（`--min`／`--zip`／`--list`）。
它的檔案清單是**當場從 `index.html` 讀**的，所以加了新的 `.js` 不必回頭改它——
但**別把清單改成寫死的**，那就是這支腳本存在的理由（見〈匯出一包給別人〉）。

## 寫文件

**做完一件事，把「為什麼」寫進 `開發筆記.md`。** 這是這個 repo 最重要的習慣，
也是它能一路改到 v1.183 還敢動的原因。寫的時候：

- 一件事一節（`###`），段名寫**人話**，因為之後是靠 grep 段名找回來的。
- **要有實測數字**：改之前多少、改之後多少、拿什麼量的。
- **試錯也要寫**：走過的死路、為什麼不行，比結論有用。
- 使用者原話用引用框留著（`> 使用者：「…」`），之後才分得出哪些是他定的、哪些是你猜的。
- 程式裡要回指的話寫 `見 開發筆記〈段名〉`（現有的 30 處都是這個格式）。

`README.md` 只有在**玩法、檔案清單、指令**真的變了才動。

## 環境

- Windows／PowerShell。路徑有中文，Bash 呼叫時一律加雙引號；優先用 PowerShell 工具。
- **中文不要直接 print 到終端**（cp950 會亂碼），導向檔案再用 Read／Grep 讀。
- 暫存檔放 scratchpad，不要丟進 repo；`tools/.e2e-out/`（截圖、預覽頁）已 gitignore，
  那裡的東西是給使用者看的，不要當成產物 commit。
- `three.js-master/` 不進 git（576 MB），只有換 three 版本時才需要它。
