# e2e 測試產物（截圖）
tools/.e2e-out/

# three.js 完整原始碼（576MB）——只有重新打包 lib/three.min.js 時才需要，
# 平常玩／跑測試都用不到。要重打包的話自己去 threejs.org 下載解壓到這裡。
three.js-master/

# cannon-es（3D 剛體物理引擎）的完整原始碼——v1.73 評估「水能不能用它做」時放進來看的。
# 結論是不用（它的 SPH 流體是 O(N²)、官方 demo 只有 240 顆粒子，而一杯水是 2300 格；
# 而且 dist 是 ES module，這個遊戲要能 file:// 直接開）。留著當參考，不進版控。
cannon-es-master/
